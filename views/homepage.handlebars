<!--Group: Current Logged-in Group-->
<!--roommateGroup: List of all OTHER roommate groups with accounts-->


<!--1. Plan on implementing banner on top w/ group profile picture (canvas) based off groupID-->




<!--2. Plan on implementing Welcome message with group name and maybe each of their profile pics lined up? -->
<h1>Welcome {{user.firstName}} {{user.lastName}}</h1>
<img src = "{{user.picture}}"width="300" height="300" class = "center">


<!-- Renders ALL profile pictures together-->
{{#each group.users}}
  {{picture}}
{{/each}}

<!--3. Plan on implementing edit profile menu which will allow user to update group info, etc. -->
{{!-- <a href="/settings">Edit User/Group Profile</a> --}}


<!--4. Plan on drop-down menu with different sorting methods (age, gender, etc.) -->
<form action="/" method="POST">
  <label for="filter">Filter Options:</label>
  <select id="filter" name="filter">
    <option value="age">Age</option>
    <option value="male">Only Males</option>
    <option value="female">Only Females</option>
    <option value="other">Only Other</option>
    <option value="budget">Budget</option>
  </select>

  <button type="submit">Submit</button>
</form>


<!--5. Plan on adding gear icon on top right corner of menu which will render the view of user to settings.handlebars -->
<div class="settingsContainer"><a href="/settings">Settings</a></div>

<div class="messagesContainer"><a href="/messages">Messages</a></div>

<!--Before rendering homepage, pass handlebars parameter with ALL ROOMMATE GROUPS-->
<!--6. Each roomante group will have a rectangle that shows their group profile page, their bio, and member information -->
<!-- ^ CSS STUFF (IGNORE)-->

<!--Need to implement feature where it only shows groups that are looking 
for group members that are less than: (4 - roommateGroup.users.length) -->

<h1>List of All Roommate Groups</h1>
{{#each roommateGroup}}
  <h1>{{groupName}} </h1>
  <h2>{{groupDescription}}</h2>
  <h2>{{groupLocation}}</h2>

  {{#each users}}
    <h3>{{firstName}} {{lastName}} {{gender}} {{biography}} {{age}} {{interests}}</h3>
  {{/each}}

  {{#if roommateGroup.reviews}}
    {{#each roommateGroup.reviews}}
      <h3>{{groupID}}</h3>
      <h3>{{firstName}} {{lastName}}</h3>
      <h3>{{review}}</h3>
  {{/each}}
  {{/if}}

<a ref="/groupbyID/{{groupID}}">View profile page</h1>
{{/each}}


